#include "main.h"

/**
 * _islower - function that checks for lowercase character
 * @c: it is a character to be checked
 * Return: 1 if c is lowecase otherwise 0.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
