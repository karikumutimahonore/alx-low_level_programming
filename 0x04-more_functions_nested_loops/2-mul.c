#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first variable
 * @b: second variable
 *
 * Return: reslt
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
